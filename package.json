{
    "name": "converto",
    "version": "1.0.0",
    "description": "",
    "scripts": {
      "prepare": "husky install",
      "dev": "docker compose up --build",
      "prod": "docker compose --env-file .env.prod -f docker-compose.prod.yml up -d",
      "test": "jest --config ./api/test/jest-e2e.json"
    },
    "devDependencies": {
      "husky": "^8.0.3"
    }
  } 